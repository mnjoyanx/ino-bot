# npx lint-staged#!/bin/bash

# Increment the version using npm version patch (increments the patch version)
npm version patch --no-git-tag-version

# change the version in version.json
echo "{\"version\": \"$(npm version --no-git-tag-version --no-workspaces --json | jq -r '.version')\"}" > version.json
